# 机器学习训练
机器学习过程的关键部分是训练模型。 在此任务中，您将在三个训练数据集上创建和训练所有模型。

## 任务3
使用sklearn库中的LogisticRegressionCV类来创建模型。 训练本身是通过应用fit函数完成的，它需要将数据拆分为特征和标签列。 您可以在下面看到如何为整个数据集执行此操作的示例。
```
features = data[['x1','x2']]
labels = data['label']
```
